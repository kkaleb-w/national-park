name: Deploy to GitHub Pages

on:
  push:
    branches: "main" # Change to "master" if your default branch is master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: ./frontend/package-lock.json # Points to the folder
          
      - name: Install Dependencies
        run: npm ci
        working-directory: ./frontend # Runs inside frontend folder
        
      - name: Build App
        run: npm run build
        working-directory: ./frontend # Runs inside frontend folder
        
      - name: Upload Artifacts
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./frontend/build # Change to ./frontend/dist if using Vite instead of Create React App

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        
    permissions:
      pages: write
      id-token: write
